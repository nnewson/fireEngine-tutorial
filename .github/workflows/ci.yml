name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  # -------- Platform-independent lint gates (run once) --------

  clang-format:
    name: clang-format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install clang-format-22
        run: |
          sudo apt-get update
          sudo apt-get install -y wget lsb-release software-properties-common
          wget -qO - https://apt.llvm.org/llvm-snapshot.gpg.key | sudo apt-key add -
          sudo add-apt-repository "deb http://apt.llvm.org/$(lsb_release -cs)/ llvm-toolchain-$(lsb_release -cs)-22 main"
          sudo apt-get update
          sudo apt-get install -y clang-format-22
      - name: Check formatting
        run: |
          find src -type f \( \
            -name '*.hpp' -o -name '*.cpp' -o -name '*.h' -o -name '*.cxx' \
          \) -print0 | xargs -0 clang-format-22 --dry-run -Werror

  clang-tidy:
    name: clang-tidy
    runs-on: ubuntu-latest
    env:
      VCPKG_ROOT: ${{ github.workspace }}/vcpkg
    steps:
      - uses: actions/checkout@v4

      - name: Install system packages
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build cmake clang-tidy

      - name: Checkout vcpkg
        uses: actions/checkout@v4
        with:
          repository: microsoft/vcpkg
          path: vcpkg

      - name: Bootstrap vcpkg
        run: ./vcpkg/bootstrap-vcpkg.sh

      - name: Cache vcpkg packages
        uses: actions/cache@v4
        with:
          path: |
            build/vcpkg_installed
            vcpkg/downloads
            vcpkg/buildtrees
            vcpkg/packages
          key: ${{ runner.os }}-vcpkg-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json') }}
          restore-keys: |
            ${{ runner.os }}-vcpkg-

      - name: Configure
        run: cmake --preset vcpkg -DFIRE_ENGINE_TUTORIAL_WARNINGS_AS_ERRORS=ON

      - name: Build
        run: cmake --build --preset default

      - name: clang-tidy
        run: clang-tidy -p build src/main.cpp

  # -------- Per-platform build + test --------

  build-test-linux:
    name: build and test (Linux)
    runs-on: ubuntu-latest
    env:
      VCPKG_ROOT: ${{ github.workspace }}/vcpkg
    steps:
      - uses: actions/checkout@v4

      - name: Install system packages
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build cmake mesa-vulkan-drivers

      - name: Checkout vcpkg
        uses: actions/checkout@v4
        with:
          repository: microsoft/vcpkg
          path: vcpkg

      - name: Bootstrap vcpkg
        run: ./vcpkg/bootstrap-vcpkg.sh

      - name: Cache vcpkg packages
        uses: actions/cache@v4
        with:
          path: |
            build/vcpkg_installed
            vcpkg/downloads
            vcpkg/buildtrees
            vcpkg/packages
          key: ${{ runner.os }}-vcpkg-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json') }}
          restore-keys: |
            ${{ runner.os }}-vcpkg-

      - name: Configure
        run: cmake --preset vcpkg -DFIRE_ENGINE_TUTORIAL_WARNINGS_AS_ERRORS=ON

      - name: Build
        run: cmake --build --preset default

      - name: Test
        run: ctest --preset default

  # GitHub's hosted macOS runner does not expose a Metal device to MoltenVK.
  # This job still verifies the macOS/AppleClang C++23 build and Vulkan linking.
  build-macos:
    name: build (macOS)
    runs-on: macos-latest
    env:
      VCPKG_ROOT: ${{ github.workspace }}/vcpkg
    steps:
      - uses: actions/checkout@v4

      - name: Install ninja
        run: brew install ninja

      - name: Checkout vcpkg
        uses: actions/checkout@v4
        with:
          repository: microsoft/vcpkg
          path: vcpkg

      - name: Bootstrap vcpkg
        run: ./vcpkg/bootstrap-vcpkg.sh

      - name: Cache vcpkg packages
        uses: actions/cache@v4
        with:
          path: |
            build/vcpkg_installed
            vcpkg/downloads
            vcpkg/buildtrees
            vcpkg/packages
          key: ${{ runner.os }}-vcpkg-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json') }}
          restore-keys: |
            ${{ runner.os }}-vcpkg-

      - name: Configure
        run: cmake --preset vcpkg -DFIRE_ENGINE_TUTORIAL_WARNINGS_AS_ERRORS=ON

      - name: Build
        run: cmake --build --preset default

  # GitHub's hosted Windows runner has no Vulkan ICD. This job still verifies
  # the Windows/MSVC C++23 build and that the executable links to Vulkan.
  build-windows:
    name: build (Windows)
    runs-on: windows-latest
    env:
      VCPKG_ROOT: ${{ github.workspace }}\vcpkg
    steps:
      - uses: actions/checkout@v4

      - name: Enable the MSVC developer environment
        uses: ilammy/msvc-dev-cmd@v1

      - name: Install ninja
        run: choco install ninja --yes --no-progress

      - name: Checkout vcpkg
        uses: actions/checkout@v4
        with:
          repository: microsoft/vcpkg
          path: vcpkg

      - name: Bootstrap vcpkg
        run: .\vcpkg\bootstrap-vcpkg.bat

      - name: Cache vcpkg packages
        uses: actions/cache@v4
        with:
          path: |
            build/vcpkg_installed
            vcpkg/downloads
            vcpkg/buildtrees
            vcpkg/packages
          key: ${{ runner.os }}-vcpkg-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json') }}
          restore-keys: |
            ${{ runner.os }}-vcpkg-

      - name: Configure
        run: cmake --preset vcpkg -DFIRE_ENGINE_TUTORIAL_WARNINGS_AS_ERRORS=ON

      - name: Build
        run: cmake --build --preset default
